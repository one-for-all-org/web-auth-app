import { NavbarProfile } from "../features/userProfile/components";

export const ProfilePage = () => {
    return (
        <div className="w-full h-auto">
            <NavbarProfile />
        </div>
    );
};
